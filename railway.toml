[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn trimed_backend.wsgi:application"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10